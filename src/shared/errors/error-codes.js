const genericPrefix = 'GNC';
const insuranceAdvicePrefix = 'ADV';
const scorePrefix = 'SCR';

module.exports = Object.freeze({
  // GENERIC
  SCHEMA_VALIDATION: `${genericPrefix}001`,

  // INSURANCE ADVICE
  UNPROCESSABLE_SCORE_VALUE: `${insuranceAdvicePrefix}0001`,

  // SCORE
});
